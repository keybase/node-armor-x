// Generated by IcedCoffeeScript 108.0.11
(function() {
  var StreamDecoder, StreamEncoder, desired_block_size, enc, stream,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  stream = require('stream');

  enc = require('./encoding');

  desired_block_size = 4096;

  exports.StreamEncoder = StreamEncoder = (function(_super) {
    __extends(StreamEncoder, _super);

    function StreamEncoder(encoder) {
      this.encoder = encoder;
      StreamEncoder.__super__.constructor.call(this, {
        highWaterMark: encoder.in_block_len * Math.ceil(desired_block_size / encoder.in_block_len)
      });
    }

    StreamEncoder.prototype._transform = function(chunk, encoding, callback) {
      this.push(this.encoder.encode(chunk));
      return callback();
    };

    return StreamEncoder;

  })(stream.Transform);

  exports.StreamDecoder = StreamDecoder = (function(_super) {
    __extends(StreamDecoder, _super);

    function StreamDecoder(encoder) {
      this.encoder = encoder;
      StreamDecoder.__super__.constructor.call(this, {
        highWaterMark: encoder.in_block_len * Math.ceil(desired_block_size / encoder.out_block_len)
      });
    }

    StreamDecoder.prototype._transform = function(chunk, encoding, callback) {
      this.push(this.encoder.decode(chunk));
      return callback();
    };

    return StreamDecoder;

  })(stream.Transform);

}).call(this);
